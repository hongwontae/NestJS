1. @Param()
1-1. 인자가 없다면 모든 URL 경로 변수를 객체 형태로 변환합니다. ('user/:id/:role')
=> {id : 2, role : 'Koala3121'}
1-2. 인자가 있다면 해당 값만 가져와서 변수에 할당합니다.


2. @Query()
2-1. 쿼리 문자열에 포함된 데이터를 가져올 떄 사용합니다.
2-2. @Query() 인자 x 라면 모든 쿼리 문자열을 key-value 객체로 받습니다.
2-3. @Query('name') 처럼 인자가 있다면 값만 가져와서 변수에 할당합니다.